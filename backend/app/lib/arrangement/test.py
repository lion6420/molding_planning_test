import pandas as pd
import numpy as np
import os
import datetime
# a = datetime.datetime.strptime('17:30', '%H:%M')
# b = datetime.timedelta(hours=3, minutes=20)
# add = a+b
# print(add.time())

a = np.nan
if a == np.nan:
    print(a)

